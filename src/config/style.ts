import { Dancing_Script, Philosopher, Caveat } from "next/font/google";

export const DancingScript = Dancing_Script({
  subsets: ["latin"],
});

export const PhilosopherFont = Philosopher({
  subsets: ["latin"],
  weight: ["400", "700"]
})

export const CaveatFont = Caveat({
  subsets: ["latin"],
  weight: ["400", "700"]
})
